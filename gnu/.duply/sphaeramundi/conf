GPG_KEY='773E854A'

TARGET='file:///Backups/'

SOURCE='/'

MAX_AGE=3M
MAX_FULL_BACKUPS=2
MAX_FULLS_WITH_INCRS=1
MAX_FULLBKP_AGE=1M
DUPL_PARAMS="$DUPL_PARAMS --full-if-older-than $MAX_FULLBKP_AGE " 

for FILENAME in .duplicity-ignore .git .bzr .svn; do
    DUPL_PARAMS="$DUPL_PARAMS --exclude-if-present '$FILENAME' "
done
